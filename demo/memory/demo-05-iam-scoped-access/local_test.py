"""Demo 5: No local test — this demo exercises IAM policies directly."""

print("Demo 5 does not use a local agent server.")
print("It demonstrates IAM scoped access directly via SDK calls.")
print("Run: python deploy.py → python invoke.py")
